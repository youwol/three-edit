//import * as Wasm from '../../../mesh'

// See https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/WebAssembly/instantiateStreaming

const vertices = [238.80174, 1176.4728, -1212.8715]
const triangles = [0, 1, 2]

test('load wasm', () => {
    // Wasm.default().then(mesh => {
    //     //console.log(mesh.borders(vertices, triangles))
    //     console.log(mesh)
    // })
})
